"use client"
import type { ReactNode, FC } from "react"
import "iconify-icon"
export const ClientResources: FC<{ children?: ReactNode }> = ({ children }) => {
    return <>
        {children}
    </>
}
